Token Size Limit
Maximum token length is 200 bytes.
Tokens longer than 200 bytes will be truncated or cause undefined behavior.
This limit applies both to commands (client→server) and responses (server→client).

/**
 * @brief Register a command handler
 * @param handler Function that returns a String response
 * @note Handler MUST return a valid String (use "" for empty response)
 * @warning Returning nullptr or having no return statement causes undefined behavior
 * 
 * Example:
 *   server.onCommand("PING", [](const String&, const String&) { return "PONG"; });
 *   server.onCommand("LOG", [](const String&, const String& msg) { 
 *       Serial.println(msg); 
 *       return "";  // Empty response
 *   });
 */
 